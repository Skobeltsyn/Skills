Cite the id of the task in `4-tasks/` that produced this result.

Record what was completed **and what was deferred** — a result that silently
drops part of its task's scope reads as done when it isn't.
