Every result links back to the task in ../4-tasks/ that produced it. A
deliverable with no task behind it is untraceable and does not belong here.

Record what was completed and what was deferred — a result that silently drops
part of its task's scope reads as done when it isn't.
