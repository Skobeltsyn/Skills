Review the evaluated results before they ship: vulnerabilities, secrets,
dependency risks, and access/permission concerns.

Record each finding with its severity and how it was resolved.

An item passes only when it clears review. Nothing reaches ../8-deploy/ with an
open finding.
