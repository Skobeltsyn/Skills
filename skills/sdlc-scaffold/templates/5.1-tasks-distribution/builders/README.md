# Builders

Who is available to build, and what each of them can take on. One folder per
builder — human or agent — holding their profile.

This is the input a distribution pass matches tasks against. It is only as good
as it is current: a stale profile produces a confident, wrong assignment.
