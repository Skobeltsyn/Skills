# Principles

The procedures this stage runs, kept separate from its law. `../AGENTS.md` says
what is always true of an assignment; a principle here says in what order to do
it — the same split the pipeline root makes between `AGENTS.md` and `RUNBOOK.md`.

- [`BUILDER_CHOOSING.md`](BUILDER_CHOOSING.md) — how a task gets matched to a
  builder: filter, sort, take the first row, and never rank.
