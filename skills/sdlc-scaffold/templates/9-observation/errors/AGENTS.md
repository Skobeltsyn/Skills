Production failures and incidents that demand action. Highest severity.

One signal per file.

These cross back into ../../1-business-tasks/observation/errors/ first — same
severity on the other side of the loop.
