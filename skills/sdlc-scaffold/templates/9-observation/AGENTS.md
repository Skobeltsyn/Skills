Watch the deployed system from ../8-deploy/ and triage every signal by severity
into errors/, warnings/, infos/.

This stage closes the loop: a signal crosses back into
../1-business-tasks/observation/ to become the next round of work, and it keeps
its severity as it crosses — errors to errors/, warnings to warnings/, infos to
infos/.
