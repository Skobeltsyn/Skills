A vibe is unstructured on purpose. Capture it — do not formalise it here.

Nothing leaves this stage as a vibe. The only exit is `prd/PRD.md`.

Two halves feed it: `raw/` holds real-world data as it arrives, and `prd/` holds
the single document that answers to it. Each states its own rules.
