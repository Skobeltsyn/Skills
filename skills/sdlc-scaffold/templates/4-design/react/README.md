# React

React implementations of the components in `../figma/`.

Every component ships with its own Storybook story (`*.stories.*`) covering all
of its internal states, at every size in `../CLAUDE.md`.

Delete this folder if the project has no React target.
