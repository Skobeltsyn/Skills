TYPE is ERROR.

Highest severity: things that are broken and demand action — failures,
incidents, blockers. These typically become tasks quickly.

If it still works, it is not an error. Put it in ../warnings/ or ../infos/.
