TYPE is WARNING.

Risks and degradations worth attention before they become errors: trends going
the wrong way, near-misses, and things that still work but shouldn't be trusted.

If it is already broken it belongs in ../errors/; if nothing is wrong at all, in
../infos/.
