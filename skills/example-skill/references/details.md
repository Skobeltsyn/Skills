# Details

This file holds the depth that doesn't belong in `SKILL.md` — long option
tables, background, worked examples, edge cases. The agent loads it only when
the main instructions point here, keeping the base token cost low.

Delete this file (or replace its contents) when you build a real skill.
