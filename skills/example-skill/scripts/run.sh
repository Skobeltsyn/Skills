#!/usr/bin/env bash
# Example helper script. Ship mechanical, deterministic steps as scripts like
# this so the agent runs them instead of re-deriving them in prose.
set -euo pipefail

echo "Replace this with the skill's actual work."
