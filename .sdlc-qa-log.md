# sdlc-scaffold — Q&A log

Design decisions for `skills/sdlc-scaffold`, captured as they were made.
Appended automatically by the hooks in `.claude/settings.local.json`.
Entries below this line were backfilled from the session transcript.

---

## Round — questions

**Q (Probe):** The probe is what separates a hypothesis from evidence. How do you want to handle it?

**Q (Fixes):** Which fixes should I draft? Cutting one leg per path is the goal — and given how much you'd lose by cutting legs 1 or 2, most of these gate leg 3.

## Round — questions

**Q (Renumbering):** SKILL.md §1 warns against renumbering, but the working tree is one. Which is it?

**Q (README):** Should README.md be required in every folder, the way AGENTS.md and CLAUDE.md are? Root and 3-design/figma/ currently lack one.

**Q (Import chain):** The per-folder CLAUDE.md stub rests on a claim about Claude Code's loading behavior. Was that verified?

**Q (Audience):** Who is this skill for? It ships in a public marketplace but is dense with origin-project specifics (Roboto, min-14px, `xs md lg xl xxl`, 26 EVT files, a Vue design system).

## Round — questions

**Q (Storybook rule):** Where should the Storybook rule live? (It's currently in 4 files, with two different bars.)

**Q (The bar):** Which bar is actually correct — the two homes disagree and I can't tell which you meant?

**Q (react/ + vue/):** If the Storybook rule moves up, react/AGENTS.md and vue/AGENTS.md have nothing framework-specific left ("This is mirror of Figma design system in react" — which is also the parent's job). SKILL.md §2 says a folder with nothing to say shouldn't exist.

**Q (Loose ends):** Two loose ends from earlier — how do you want them handled?

---

## Answers given

- "SKILL.md §1 warns against renumbering
- but the working tree is one. Which is it?"="remove this part"
- "Should README.md be required in every folder
- the way AGENTS.md and CLAUDE.md are? Root and 3-design/figma/ currently lack one."="Oversight — all 28 need one"
- "The per-folder CLAUDE.md stub rests on a claim about Claude Code's loading behavior. Was that verified?"="Doesn't matter — tool-neutrality is the point"
- "Who is this skill for? It ships in a public marketplace but is dense with origin-project specifics (Roboto
- min-14px
- `xs md lg xl xxl`
- 26 EVT files
- a Vue design system)."="Generic structure
- opinionated defaults". You can now continue with these answers in mind.
- "Where should the Storybook rule live? (It's currently in 4 files
- with two different bars.)"="Leave it — redundancy is insurance"
- "Which bar is actually correct — the two homes disagree and I can't tell which you meant?"="Every internal state (the leaves)"
- "If the Storybook rule moves up
- react/AGENTS.md and vue/AGENTS.md have nothing framework-specific left ("This is mirror of Figma design system in react" — which is also the parent's job). SKILL.md §2 says a folder with nothing to say shouldn't exist."="They're placeholders and that's fine"
- "Two loose ends from earlier — how do you want them handled?"="Retry the Q&A hooks here
- Drop the stage-conventions reference
- Write the stage-conventions skill". You can now continue with these answers in mind.
